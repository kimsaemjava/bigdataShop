<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.kitri.product">
<select id="mainhitproduct" resultType="product">
	<![CDATA[select p.*,sort_table.sumqt from tb_product p,(select prd_no,sum(qty) sumqt from tb_order_product group by prd_no order by sumqt desc) sort_table where p.prd_no=sort_table.prd_no and rownum<=3]]>
</select>
<select id="mainnewproduct" resultType="product">
	<![CDATA[select * from tb_product where rownum<=3 order by reg_dtm desc]]>
</select>
<select id ="allproduct" resultType="product" parameterType="String">
	select * from tb_product where category_no = #{category}
</select>
</mapper>